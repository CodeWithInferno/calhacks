data_path: data/demo_slope_load_data.csv
window_size: 10

model_type: gru
hidden_dim: 256
num_layers: 2
dropout: 0.2

batch_size: 512
epochs: 100
learning_rate: 0.001
weight_decay: 0.0001

output_dir: models/medium
patience: 15
val_frac: 0.1
save_every: 5
