data_path: data/demo_slope_load_data.csv
window_size: 10

model_type: gru
hidden_dim: 512
num_layers: 3
dropout: 0.3

batch_size: 256
epochs: 100
learning_rate: 0.001
weight_decay: 0.0001

output_dir: models/large
patience: 15
val_frac: 0.1
save_every: 5
