fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
boto3>=1.34
# For the trained fall-risk GRU
torch>=2.0
numpy>=1.24
pandas>=2.0
scipy>=1.10
pyyaml>=6.0
# MuJoCo is optional; the backend falls back to a stub walk if it is unavailable.
# mujoco>=3.0
