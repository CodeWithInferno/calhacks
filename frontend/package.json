{
  "name": "robot-rollout-viewer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Browser viewer for Isaac Lab robot rollouts stored in Nebius Object Storage (S3-compatible).",
  "scripts": {
    "fetch": "node scripts/fetch-run.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.184.0",
    "urdf-loader": "^0.12.6"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/credential-providers": "^3.700.0",
    "dotenv": "^16.4.5",
    "vite": "^6.0.0"
  }
}
